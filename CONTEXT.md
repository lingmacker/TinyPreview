# TinyPreview

TinyPreview 增强 Finder 中由空格键触发的快速预览体验。

## Language

**快速预览**：
用户在 Finder 中选中项目并按空格后出现的临时预览界面。
_Avoid_：独立预览、应用内浏览

**容器项目**：
内容由多个子项目构成的文件夹或压缩包。
_Avoid_：集合文件、目录包

**内容清单**：
容器项目中子项目的只读层级摘要；它可以因安全预算而不完整，用户不能从清单继续打开或预览单个子项目。
_Avoid_：文件浏览器、目录浏览

**预览大小上限**：
用户按预览分类配置的所选文件逻辑大小阈值；超过对应阈值的文件不进入内容预览。文件夹不适用该阈值。
_Avoid_：解压后大小限制、内存限制

**安全预算**：
预览过程中不可关闭的资源边界，包括解码内存、容器条目数和处理时长；它独立于预览大小上限。
_Avoid_：文件大小配置

**纯文本项**：
内容可完整解码为 UTF‑8，或带 BOM 的 UTF‑16/UTF‑32，且不含 NUL、非排版控制字符不超过 1% 的文件；该判定与文件名和后缀无关。
_Avoid_：可打印文件、任意可解码文件

**源代码项**：
可由标准后缀、知名文件名或 shebang 可靠判定编程语言的纯文本项；无法可靠判定语言时仍是纯文本项。
_Avoid_：疑似代码、关键字文本

**图像项**：
当前 macOS 能完整识别并解码其内容的图片，或内容有效的 SVG；是否属于图像项不由文件后缀单独决定。PDF 不属于图像项。
_Avoid_：所有图片格式、图片后缀文件

**预览分类**：
根据项目实际内容选择唯一预览形态的判定结果；文件名、后缀和系统声明类型只提供辅助信号。
_Avoid_：文件类型、后缀类型

**预览拒绝**：
项目的预览分类已确定，但因预览大小上限或安全预算而不展示内容的结果；拒绝原因必须与不支持相区分。
_Avoid_：预览失败、格式不支持

**不支持项**：
TinyPreview 无法归入容器项目、纯文本项、源代码项或图像项的所选项目；它与已分类但被拒绝的项目不同。
_Avoid_：预览拒绝、预览错误

**预览错误**：
项目已有可靠预览分类，但其内容损坏或无法按该分类完成解析的结果；预览错误不降级为低优先级分类。
_Avoid_：不支持项、预览拒绝
